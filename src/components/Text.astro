---
const { style = "p", stylingclasses = "" } = Astro.props;

const sizeClasses = {
  p: "text-[16px] md:text-[22px] font-sans",

  // bold + italic tekst (emphasis)
  pEm: "text-[16px] md:text-[22px] font-sans-bold italic",
  psmall: "text-[12px] md:text-[18px] font-sans",
};
---

<p class:list={[sizeClasses[style], stylingclasses]}>
  <slot />
</p>
